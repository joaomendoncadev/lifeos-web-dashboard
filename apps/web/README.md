# @lifeos/web

Frontend Next.js do LifeOS. A aplicação consome a API Spring Boot indicada por `NEXT_PUBLIC_API_URL`.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Consulte o README da raiz para executar a stack completa com Podman.
